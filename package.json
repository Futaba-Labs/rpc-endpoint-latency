{
  "name": "rpc-endpoint-latency",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start:provider": "pnpm run build && node dist/provider.js",
    "start:polling": "pnpm run build && node dist/polling.js",
    "start:websocket": "pnpm run build && node dist/websocket.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.5.4",
    "command-line-args": "^6.0.0",
    "typescript": "^5.5.4",
    "viem": "^2.21.3"
  }
}
